name: Code Automation

on:
  issues:
    types: [opened, edited, labeled]
  workflow_run:
    workflows: ["Code Automation"]
    types:
      - completed

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  initial-setup:
    runs-on: ubuntu-latest
    if: github.event_name == 'issues' && contains(github.event.issue.labels.*.name, 'feature-request')
    outputs:
      needs_fix: ${{ steps.check_deps.outputs.needs_fix }}
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
          token: ${{ secrets.FINANCEAPP_TOKEN }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18'
      
      - name: Check Dependencies
        id: check_deps
        run: |
          if [ ! -f "package.json" ] || [ ! -f "package-lock.json" ]; then
            echo "needs_fix=true" >> $GITHUB_OUTPUT
          else
            echo "needs_fix=false" >> $GITHUB_OUTPUT
          fi

  fix-dependencies:
    needs: initial-setup
    runs-on: ubuntu-latest
    if: needs.initial-setup.outputs.needs_fix == 'true'
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
          token: ${{ secrets.FINANCEAPP_TOKEN }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18'
      
      - name: Fix Dependencies
        run: |
          if [ ! -f "package.json" ]; then
            echo "Creating package.json..."
            echo '{
              "name": "finance-app",
              "version": "0.1.0",
              "private": true,
              "dependencies": {
                "@testing-library/jest-dom": "^5.17.0",
                "@testing-library/react": "^13.4.0",
                "@testing-library/user-event": "^13.5.0",
                "react": "^18.2.0",
                "react-dom": "^18.2.0",
                "react-scripts": "5.0.1",
                "web-vitals": "^2.1.4",
                "@mui/material": "^5.15.11",
                "@mui/icons-material": "^5.15.11",
                "@emotion/react": "^11.11.3",
                "@emotion/styled": "^11.11.0"
              },
              "scripts": {
                "start": "react-scripts start",
                "build": "react-scripts build",
                "test": "react-scripts test",
                "eject": "react-scripts eject"
              }
            }' > package.json
          fi

          echo "Installing dependencies..."
          npm install --legacy-peer-deps
          
          echo "Committing changes..."
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add package.json package-lock.json
          git commit -m "fix: Dependencies added and lock file generated"
          git push origin ${GITHUB_REF#refs/heads/}

  generate-code:
    needs: [initial-setup, fix-dependencies]
    if: |
      always() &&
      (needs.initial-setup.result == 'success' || needs.fix-dependencies.result == 'success') &&
      github.event_name == 'issues' &&
      contains(github.event.issue.title, 'Giriş sayfası')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
          token: ${{ secrets.FINANCEAPP_TOKEN }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18'
      
      - name: Install Dependencies
        run: npm install --legacy-peer-deps
      
      - name: Generate Login Page
        run: |
          mkdir -p src/components/auth
          cat > src/components/auth/Login.js << 'EOL'
          import React, { useState } from 'react';
          import { Box, Button, TextField, Typography, Container, Link } from '@mui/material';

          const Login = () => {
            const [formData, setFormData] = useState({
              email: '',
              password: ''
            });

            const handleChange = (e) => {
              setFormData({
                ...formData,
                [e.target.name]: e.target.value
              });
            };

            const handleSubmit = (e) => {
              e.preventDefault();
              console.log('Form submitted:', formData);
            };

            return (
              <Container component="main" maxWidth="xs">
                <Box
                  sx={{
                    marginTop: 8,
                    display: 'flex',
                    flexDirection: 'column',
                    alignItems: 'center',
                  }}
                >
                  <Typography component="h1" variant="h5">
                    Giriş Yap
                  </Typography>
                  <Box component="form" onSubmit={handleSubmit} sx={{ mt: 1 }}>
                    <TextField
                      margin="normal"
                      required
                      fullWidth
                      id="email"
                      label="E-posta Adresi"
                      name="email"
                      autoComplete="email"
                      autoFocus
                      value={formData.email}
                      onChange={handleChange}
                    />
                    <TextField
                      margin="normal"
                      required
                      fullWidth
                      name="password"
                      label="Şifre"
                      type="password"
                      id="password"
                      autoComplete="current-password"
                      value={formData.password}
                      onChange={handleChange}
                    />
                    <Button
                      type="submit"
                      fullWidth
                      variant="contained"
                      sx={{ mt: 3, mb: 2 }}
                    >
                      Giriş Yap
                    </Button>
                    <Box sx={{ textAlign: 'center' }}>
                      <Link href="#" variant="body2">
                        Şifremi Unuttum
                      </Link>
                      <Box sx={{ mt: 1 }}>
                        <Link href="#" variant="body2">
                          Hesabınız yok mu? Kayıt Ol
                        </Link>
                      </Box>
                    </Box>
                  </Box>
                </Box>
              </Container>
            );
          };

          export default Login;
          EOL

          git add .
          git commit -m "feat: Giriş sayfası oluşturuldu"
          git push origin ${GITHUB_REF#refs/heads/}

      - name: Create PR
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.FINANCEAPP_TOKEN }}
          commit-message: "feat: ${{ github.event.issue.title }} implementasyonu"
          title: "✨ Feature: ${{ github.event.issue.title }}"
          body: |
            Closes #${{ github.event.issue.number }}
            
            ## 📝 Implementation Details
            ${{ github.event.issue.body }}
            
            ## 🔍 Review Points
            - [ ] Feature requirements met
            - [ ] Tests added
            - [ ] Documentation updated
          branch: "feature/${{ github.event.issue.title }}"
          base: main 